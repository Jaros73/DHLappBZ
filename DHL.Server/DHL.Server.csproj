<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Swashbuckle.AspNetCore" Version="6.6.2" />
  </ItemGroup>
	<Target Name="CopyBlazorClient" AfterTargets="Publish">
		<Exec Command="dotnet publish ../DHL.Client/DHL.Client.csproj -c $(Configuration) -o $(PublishDir)\wwwroot" />
	</Target>
</Project>
